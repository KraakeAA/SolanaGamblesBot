{
  "name": "solana-casino-bot",
  "version": "2.0.0",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dashboard": "node dashboard.js",
    "dev": "nodemon index.js",
    "test": "jest --verbose"
  },
  "dependencies": {
    "@solana/web3.js": "^1.93.0",
    "@solana/spl-token": "^0.4.2", // Kept for now, assuming it might be used elsewhere or planned. Remove if certain it's unused.

    "bs58": "^5.0.0",
    "base64-js": "^1.5.1", // Needed for updated index.js
    "buffer": "^6.0.3",    // Needed for updated index.js

    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "node-telegram-bot-api": "^0.61.0",
    "node-fetch": "^3.3.2", // Kept for now, remove if certain it's unused.

    "pg": "^8.11.5", // Provides Pool, used in index.js

    "p-queue": "^7.3.4",

    "winston": "^3.11.0",     // Kept for now, Logger - remove if not used.
    "prom-client": "^14.2.0" // Kept for now, Metrics - remove if not used.
  },
  "devDependencies": {
    "nodemon": "^3.0.2",
    "jest": "^29.7.0",
    "@types/jest": "^29.5.6",
    "@types/node": "^20.8.10",
    "eslint": "^8.56.0"
  },
  "engines": {
    "node": ">=18.x"
  }
}
